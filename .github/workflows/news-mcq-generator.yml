name: MCQ Generator
on:
  schedule:
    # Run every 6 hours to maximize processing time per run
    - cron: '0 */7 * * *'
  workflow_dispatch: # Allow manual triggering
  push:
    branches: [ main ]
    paths: [ 'mcq_generator.py' ] # Only run when the MCQ script changes

jobs:
  generate-mcqs:
    runs-on: ubuntu-latest
    timeout-minutes: 350 # Use almost the full 6-hour GitHub Actions limit
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
        
    - name: Pull latest changes
      run: |
        echo "Pulling latest changes before starting..."
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action Bot"
        git pull origin main --rebase || git pull origin main --no-rebase || echo "Pull failed, continuing..."
        
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'
        
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential cmake
        
    - name: Install Python dependencies
      run: |
        pip3 install --upgrade pip
        pip3 install llama-cpp-python
        pip3 install huggingface-hub
        
    - name: Create directories
      run: |
        mkdir -p models
        mkdir -p articles
        
    - name: Check for cached model
      id: cache-model
      uses: actions/cache@v4
      with:
        path: models/mistral-7b-instruct-v0.1.Q4_K_M.gguf
        key: mistral-model-v1
        restore-keys: |
          mistral-model-
          
    - name: Download Mistral model
      if: steps.cache-model.outputs.cache-hit != 'true'
      run: |
        cd models
        huggingface-cli download TheBloke/Mistral-7B-Instruct-v0.1-GGUF mistral-7b-instruct-v0.1.Q4_K_M.gguf --local-dir . --local-dir-use-symlinks False
      env:
        HF_HUB_DISABLE_PROGRESS_BARS: 1
        
    - name: Verify model download
      run: |
        ls -la models/
        if [ ! -f "models/mistral-7b-instruct-v0.1.Q4_K_M.gguf" ]; then
          echo "Model file not found!"
          exit 1
        fi
        echo "Model size: $(ls -lh models/mistral-7b-instruct-v0.1.Q4_K_M.gguf | awk '{print $5}')"
        
    - name: Check for articles to process
      id: check-articles
      run: |
        # Create empty file if it doesn't exist
        if [ ! -f "articles/news_articles.txt" ]; then
          echo "Creating empty articles file..."
          touch articles/news_articles.txt
        fi
        
        # Check if file has content
        if [ ! -s "articles/news_articles.txt" ]; then
          echo "Articles file is empty. Nothing to process."
          echo "has_articles=false" >> $GITHUB_OUTPUT
          exit 0
        fi
        
        # Count articles
        article_count=$(grep -c "^Link: " articles/news_articles.txt || echo "0")
        echo "Found $article_count articles to process"
        echo "has_articles=true" >> $GITHUB_OUTPUT
        echo "article_count=$article_count" >> $GITHUB_OUTPUT
        
        if [ "$article_count" -eq "0" ]; then
          echo "No articles to process."
          echo "has_articles=false" >> $GITHUB_OUTPUT
        fi
        
    - name: Show current articles status
      if: steps.check-articles.outputs.has_articles == 'true'
      run: |
        echo "=== BEFORE PROCESSING ==="
        echo "📰 Total articles in input file: $(grep -c '^Link: ' articles/news_articles.txt || echo '0')"
        echo "📝 Current quiz file size: $(wc -l < quiz.txt 2>/dev/null || echo '0') lines"
        echo "🔍 First few links to be processed:"
        grep "^Link: " articles/news_articles.txt | head -5
        
    - name: Run MCQ generator
      if: steps.check-articles.outputs.has_articles == 'true'
      run: |
        echo "Starting MCQ generation at $(date)"
        timeout 340m python3 mcq_generator.py || {
          echo "MCQ generation completed/timed out at $(date)"
        }
        echo "MCQ generation completed at $(date)"
      env:
        MODEL_PATH: ./models/mistral-7b-instruct-v0.1.Q4_K_M.gguf
        INPUT_FILE: ./articles/news_articles.txt
        
    - name: Check generated output
      if: steps.check-articles.outputs.has_articles == 'true'
      run: |
        echo "=== AFTER PROCESSING ==="
        
        if [ -f "quiz.txt" ]; then
          quiz_lines=$(wc -l < quiz.txt)
          quiz_size=$(ls -lh quiz.txt | awk '{print $5}')
          echo "📝 Quiz file: $quiz_lines lines, $quiz_size"
          
          # Count generated questions
          question_count=$(grep -c "^Q[0-9]\." quiz.txt || echo "0")
          echo "❓ Total questions generated: $question_count"
          
          echo "📋 Last few lines of quiz file:"
          tail -10 quiz.txt
        else
          echo "❌ No quiz file generated"
        fi
        
        echo ""
        if [ -f "articles/news_articles.txt" ]; then
          remaining_articles=$(grep -c "^Link: " articles/news_articles.txt || echo "0")
          echo "📰 Remaining articles: $remaining_articles"
          
          if [ "$remaining_articles" -gt "0" ]; then
            echo "🔍 Next articles to process:"
            grep "^Link: " articles/news_articles.txt | head -3
          fi
        else
          echo "📰 No articles file remaining"
        fi
        
    - name: Create conflict resolution script
      run: |
        cat > resolve_conflicts.py << 'SCRIPT_END'
        #!/usr/bin/env python3
        import os
        import sys
        
        def read_articles(filename):
            if not os.path.exists(filename):
                return []
            with open(filename, 'r', encoding='utf-8') as f:
                content = f.read()
            if not content.strip():
                return []
            articles = []
            article_blocks = content.split('\n\n')
            for block in article_blocks:
                if not block.strip():
                    continue
                lines = block.strip().split('\n')
                current_link = None
                current_article = None
                for line in lines:
                    if line.startswith("Link: "):
                        current_link = line.strip()
                    elif line.startswith("Article: "):
                        current_article = line[len("Article: "):].strip()
                if current_link and current_article:
                    articles.append((current_link, current_article))
            return articles
        
        def write_remaining_articles(filename, remaining_articles):
            with open(filename, 'w', encoding='utf-8') as f:
                for i, (link, article) in enumerate(remaining_articles):
                    f.write(f"{link}\n")
                    f.write(f"Article: {article}\n")
                    if i < len(remaining_articles) - 1:
                        f.write("\n")
        
        input_file = sys.argv[1] if len(sys.argv) > 1 else 'articles/news_articles.txt'
        batch_size = int(sys.argv[2]) if len(sys.argv) > 2 else 500
        
        all_articles = read_articles(input_file)
        remaining_articles = all_articles[batch_size:]
        write_remaining_articles(input_file, remaining_articles)
        print(f"Kept {len(remaining_articles)} articles, removed first {batch_size}")
        SCRIPT_END
        chmod +x resolve_conflicts.py

    - name: Commit and push changes
      if: steps.check-articles.outputs.has_articles == 'true'
      run: |
        echo "Checking for changes to commit..."
        
        # Add files that may have changed
        git add articles/news_articles.txt quiz.txt 2>/dev/null || true
        
        if git diff --staged --quiet; then
          echo "📝 No changes to commit"
        else
          # Create a descriptive commit message
          if [ -f "quiz.txt" ]; then
            question_count=$(grep -c "^Q[0-9]\." quiz.txt || echo "0")
          else
            question_count="0"
          fi
          
          remaining_articles=$(grep -c "^Link: " articles/news_articles.txt 2>/dev/null || echo "0")
          
          git commit -m "🤖 MCQ Generation Run #${GITHUB_RUN_NUMBER:-0} - $question_count questions, $remaining_articles articles remaining [$(date '+%Y-%m-%d %H:%M UTC')]"
          
          # Try to push with proper merge handling
          for i in {1..5}; do
            if git push; then
              echo "✅ Changes committed and pushed successfully"
              break
            else
              echo "⚠️ Push attempt $i failed, merging remote changes..."
              
              # Pull and merge remote changes
              git fetch origin main
              
              # Check if there are actual conflicts or just need to merge
              if git merge origin/main --no-edit; then
                echo "✅ Successfully merged remote changes"
              else
                echo "🔧 Merge conflicts detected, resolving..."
                
                # Check if conflict is in articles file
                if git status --porcelain | grep -q "articles/news_articles.txt"; then
                  echo "📰 Resolving conflict in news_articles.txt"
                  
                  # Use the version from origin (has new articles) and re-apply our logic
                  git checkout origin/main -- articles/news_articles.txt
                  
                  # Re-apply our processing logic to remove processed articles
                  echo "Re-applying article removals..."
                  python3 resolve_conflicts.py articles/news_articles.txt 500
                  
                  # Mark as resolved
                  git add articles/news_articles.txt
                else
                  echo "📄 Resolving other conflicts automatically"
                  # For quiz.txt conflicts, prefer our version (has new questions)
                  if git status --porcelain | grep -q "quiz.txt"; then
                    git add quiz.txt
                  fi
                fi
                
                # Complete the merge
                git commit --no-edit || git commit -m "Merge remote changes with MCQ generation updates"
              fi
              
              # Update commit message with current counts
              current_questions=$(grep -c "^Q[0-9]\." quiz.txt || echo "0")
              current_remaining=$(grep -c "^Link: " articles/news_articles.txt || echo "0")
              git commit --amend -m "🤖 MCQ Generation Run #${GITHUB_RUN_NUMBER:-0} - $current_questions questions, $current_remaining articles remaining [$(date '+%Y-%m-%d %H:%M UTC')]" || true
              
              if [ $i -eq 5 ]; then
                echo "❌ Failed to push after 5 attempts"
                exit 1
              fi
              sleep 3
            fi
          done
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: mcq-generation-${{ github.run_number }}
        path: |
          articles/
          quiz.txt
        retention-days: 7
        
    - name: Run summary
      if: always()
      run: |
        echo "=== RUN SUMMARY ==="
        echo "🚀 Workflow: ${{ github.workflow }}"
        echo "📅 Run: ${{ github.run_number }}"
        echo "⏰ Completed: $(date)"
        
        if [ -f "quiz.txt" ]; then
          total_questions=$(grep -c "^Q[0-9]\." quiz.txt || echo "0")
          echo "✅ Total questions in quiz file: $total_questions"
        fi
        
        if [ -f "articles/news_articles.txt" ]; then
          remaining=$(grep -c "^Link: " articles/news_articles.txt || echo "0")
          echo "📰 Articles remaining: $remaining"
          
          if [ "$remaining" -gt "0" ]; then
            echo "💡 Next run will process more articles"
          else
            echo "🎉 All articles have been processed!"
          fi
        fi
        
    - name: Clean up large files
      run: |
        # Clean up the model file to save space (it will be cached)
        rm -rf models/
      if: always()